fun thrice(fn) {
  for (var i = 1; i <= 3; i = i + 1) {
    fn(i);
  }
}

thrice(fun (a) {
  print a;
});
// "1".
// "2".
// "3".

// Test if expression statement is parsed as such and not as function declaration.
fun () {};

// Test assignment of anonymous function to variable.
var test = fun () { print "Assigned lambda to variable!"; };
test();
